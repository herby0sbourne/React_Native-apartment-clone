import { ViewStyle } from "react-native";

const utils: { row: ViewStyle } = {
  row: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
  },
};

export default utils;
