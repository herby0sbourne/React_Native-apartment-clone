import { StyleSheet, Text, View } from "react-native";

const Page = () => {
  return (
    <View>
      <Text>ForgotPassword</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Page;
